# Blink RGB LED demo

This demo is located in the folder ```embedded/Thijmen/blink-rgb-led```

This demo controls a single LED using the FastLED library.  
The LED blinks at a fixed interval and displays a random color each time it turns on.  
A button is used to toggle the blinking behavior on and off.